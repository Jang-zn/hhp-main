spring:
  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트 시작시 생성, 종료시 삭제
    show-sql: false  # 테스트에서는 SQL 로그 비활성화
    properties:
      hibernate:
        format_sql: true
        
# SpringDoc 테스트 설정
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false